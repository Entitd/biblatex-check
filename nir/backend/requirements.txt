# Основные зависимости FastAPI и ASGI-сервера
fastapi==0.109.1
uvicorn==0.27.0

# Работа с базой данных
sqlalchemy==2.0.25
psycopg2-binary==2.9.9  # или asyncpg для асинхронного подключения
alembic==1.13.1  # (опционально, для миграций)

# Аутентификация и хеширование паролей
passlib==1.7.4
bcrypt==4.1.2

# Работа с .env-файлами
python-dotenv==1.0.0

# Парсинг BibTeX
bibtexparser==1.4.0

# Планировщик задач (APScheduler)
apscheduler==3.10.4

# Валидация данных (Pydantic уже идёт с FastAPI)
pydantic==2.6.1

# Логирование
logging==0.4.9.6  # (обычно встроен в Python, но можно явно указать)

# Статика и файлы
aiofiles==23.2.1  # (для работы с файлами в асинхронном режиме)

# CORS (уже есть в FastAPI, но можно обновить)
starlette==0.36.3  # (FastAPI зависит от него)